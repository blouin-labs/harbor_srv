# TODO: Harden root authentication before production use.
# Current state (empty root password + password auth) is insecure.
# Recommended approach: disable password auth entirely, require SSH key.
#   - Bake an authorized_keys into the profile for the deployment key
#   - Set PasswordAuthentication no and PermitRootLogin prohibit-password
# Alternative: set a strong hashed password in /etc/shadow at build time.
PasswordAuthentication yes
PermitRootLogin yes
